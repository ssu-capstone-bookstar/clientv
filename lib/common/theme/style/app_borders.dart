import 'package:flutter/cupertino.dart';

class AppBorders {
  static const BorderRadiusGeometry BOTTOM_NAV_BAR_BORDER_RADIUS = BorderRadius.only(
    topLeft: Radius.circular(24),
    topRight: Radius.circular(24),
  );
}

