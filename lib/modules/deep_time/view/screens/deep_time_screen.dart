import 'package:flutter/material.dart';

class DeepTimeScreen extends StatelessWidget {
  const DeepTimeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        '딥타임 화면',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
