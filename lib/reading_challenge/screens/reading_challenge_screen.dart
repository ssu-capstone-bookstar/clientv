import 'package:flutter/material.dart';

class ReadingChallengeScreen extends StatelessWidget {
  const ReadingChallengeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        '리딩챌린지 화면',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
} 